# fantasy-league-compare

run as py index.py

yfa - scoring modifiers in league.settings().stat_modifiers().stats


## Dependencies
pip install yahoo-oauth

pip install yahoo-fantasy-api

// pip install yfpy